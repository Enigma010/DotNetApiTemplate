# Get a API Token
The following describes how to get an API token for using the API.

## Running the API
1. Go to the [Application](https://localhost:7035/) and login.
1. Go to the [Api token url](https://localhost:7035/api/token).  Take the value of property **token** and use the value in the **Bearer** token.
1. Use the API.
